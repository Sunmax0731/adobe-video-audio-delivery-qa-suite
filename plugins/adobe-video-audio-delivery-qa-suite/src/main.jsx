/* Adobe動画・音声納品検品スイート */
(function () {
  var profile = {
    name: "Adobe動画・音声納品検品スイート",
    host: "PremierePro",
    repository: "adobe-video-audio-delivery-qa-suite"
  };
  $.writeln(profile.name + " loaded for " + profile.host);
}());
